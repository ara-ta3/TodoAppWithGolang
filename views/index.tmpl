{{define "index.tmpl"}}
<!DOCTYPE html>
<html lang="ja">
    {{template "header" .}}
    <body>
        <div class="container">
            <div class="row">
                <div id="contents" class="col-md-12"></div>
            </div>
        </div>
        {{template "footer" .}}
    </body>
</html>
{{end}}
